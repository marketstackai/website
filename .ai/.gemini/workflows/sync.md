# AI Config Sync Workflow

## Trigger
- "Always do this at the beginning of the session"
- "When we update AI configuration, rules, or settings"
- "After refactor, run tests"

## Action
When triggered, please run:
`pnpm exec lnai sync`

This ensures that the unified LNAI configurations are propagated to all AI coding tools.
